default:
  context:
    parameters:
      url: http://localhost:8081
      documentRoot: tests/examples
      timeout: 10
      phantom_port: 8999
  paths:
    features: tests/features

standalone:
  paths:
    features: tests/features
  filters:
      tags: "~@webserver"
