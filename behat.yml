default:
  context:
    parameters:
      url: http://localhost:8000
      documentRoot: tests/examples
      timeout: 1
      phantom_port: 8999
  paths:
    features: tests/features

standalone:
  paths:
    features: tests/features
  filters:
      tags: "~@webserver"